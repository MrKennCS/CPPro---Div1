#include<iostream>
#include<queue>

using namespace std;

int main(){
    queue<int> qu;
    int n;
    cin >> n;
    int a[n + 5];
    for(int i=1; i<=n; ++i){
        cin >> a[i];
        qu.push(a[i]);
    }

    
}