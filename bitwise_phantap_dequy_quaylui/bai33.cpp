#include<iostream>
#include<vector>
#define get_bit(x, y) (((x) >> (y)) & 1)
#define ll long long
using namespace std;

int q;

void solve(){
    ll n;   cin >> n;

    

}

int main(){
    ios_base::sync_with_stdio(false);   cin.tie(0);
    cin >> q;
    while(q--)  solve();
}