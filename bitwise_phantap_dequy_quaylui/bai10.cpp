#include<iostream>
#include<vector>
#define sz(a) (int)a.size()
#define get_bit(x, y) (((x) >> (y)) % 3)

using namespace std;

int n;
int th[3] = {0, 1, 2};
vector<int> a, b;
vector<int> mask;

void quaylui(){
    for(int i=0; i<3; ++i){
        mask.push_back(th[i]);

        
    }
}

void solve(){

}

int main(){
    ios_base::sync_with_stdio(false);   cin.tie(0);
    solve();
}