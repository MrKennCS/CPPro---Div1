#include<iostream>

using namespace std;

void solve(){
    /*
    Ban đầu cho một dãy số gồm n phần tử
    ta chọn ra k cặp số thỏa mãn 5 quy tắc trong đề
        + 2 đầu hàng
        + 2 cuối hàng
        + 1 đầu 1 cuối
        + loại 1 thẻ đầu
        + loại 1 thẻ cuối
    
    => Yêu cầu chọn ra k cặp có số tiền thưởng lớn nhất

    1 cặp -> nhận được |a - b|
        */
}

int main(){
    ios_base::sync_with_stdio(false);   cin.tie(0);
    solve();
}