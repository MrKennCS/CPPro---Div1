#include<iostream>
#define ll long long

using namespace std;

int n;
ll k;
int a[100009];
int b[100009];

void solve(){
    cin >> n >> k;
    for(int i=1; i<=n; ++i) cin >> a[i];
    for(int i=1; i<=n; ++i) cin >> b[i];
    
}

int main(){
    ios_base::sync_with_stdio(false);   cin.tie(0);
    solve();
}