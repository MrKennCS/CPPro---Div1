#include<iostream>

using namespace std;

int n, c;
int x[105], y[105], sm[105], pm[105], sl[105], pl[105];
int l, r, mid, res;

void solve(){
    cin >> n >> c;
    for(int i=1; i<=n; ++i){
        cin >> x[i] >> y[i] >> sm[i] >> pm[i] >> sl[i] >> pl[i];
    }

    /*
    l = 1;
    r = 1e9;

    while(l <= r){
        mid = (l + r) >> 1;
        if( )
    }
    */

}

int main(){
    ios_base::sync_with_stdio(false);   cin.tie(0);
    solve();
}